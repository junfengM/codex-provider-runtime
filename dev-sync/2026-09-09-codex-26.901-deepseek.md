<!-- Generated by Codex on 2026-09-09. -->

# Codex Desktop 26.901.51231 DeepSeek compatibility

## 结论

DeepSeek Provider Runtime 已按 Desktop `26.901.51231` 内置 Codex CLI
`0.153.4` 重建并激活；Flash/Pro 继续路由到 `deepseek`，GPT 继续路由到
`openai`。

## 变更

- 更新补丁管理器，使旧版和 `0.153.4` 的 request-processor 模块布局都能
  fail-closed 地匹配；锚点缺失或多重匹配仍拒绝构建。
- 增加新模块布局的回归测试。
- 同步兼容矩阵、Provider Runtime skill 基线、Feature Backlog。

## 验证

- `python3 -m unittest tests/test_router_manager.py`：16 项通过。
- `codex-provider update`：构建、签名和协议 smoke 通过。
- `codex-provider verify`：ChatGPT/DeepSeek 结构校验通过。
- Desktop 重启后 app-server 实际加载
  `/Users/mjf/.codex/provider-runtime/current/codex`。
- `codex-provider appserver-smoke deepseek-v4-flash`：结构化
  `commandExecution` 成功，隐藏 SHA-256 challenge 匹配。

## 风险

模型契约仍沿用 2026-08-13 已验证的 DeepSeek V4 Flash/Pro native
Responses 路径；本次变化是上游 Codex 源码布局兼容，不是新增模型或传输层。
