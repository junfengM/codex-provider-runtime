<!-- Generated by Codex on 2026-09-10. -->

# Desktop 26.903.71938 自动重建与 DeepSeek 健康复查

## 结论

Codex Desktop 升级到 `26.903.71938`（内置 Codex 仍为 `0.153.4`，但二进制
sha256 变为 `87a08119b8ef...`）后，Provider Runtime 在 1 分钟内自动检测、
重建并激活匹配 release，DeepSeek 路由与真实请求均正常。

## 变更

- 无源码改动；本轮是运行时自愈 + 健康复查。

## 验证

- `codex-provider status` / `doctor`：release
  `0.153.4-3d2ee51ca2d5-87a08119b8ef-41c26a3e4d23` 与官方二进制 sha 一致，
  `disabled=false`。
- Desktop 进程环境 `CODEX_CLI_PATH=/Users/mjf/.codex/provider-runtime/current/codex`。
- 协议 smoke：Flash/Pro → `deepseek`，GPT → `openai`，冷恢复线程继承
  `deepseek`。
- `doctor --live`：`deepseek-v4-flash` 显式路由 + 结构化工具调用通过。
- `appserver-smoke deepseek-v4-flash`：provider `deepseek`、
  `commandExecution` 结构化、隐藏 SHA-256 challenge 匹配。

## 风险

- 沙箱内直接跑 live 检查会因 `~/.codex` 只读而假失败，需在沙箱外执行；
  该诊断陷阱已沉淀为 lessons。
- `codex doctor` 报的 MCP `https://developers.openai.com/mcp` HTTP 403
  与 DeepSeek 无关，待单独排查。
