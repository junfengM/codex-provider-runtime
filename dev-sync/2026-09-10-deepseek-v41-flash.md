<!-- Generated by Codex on 2026-09-10. -->

# 接入 DeepSeek V4.1 Flash（deepseek-flash），退役 V4-Flash

## 结论

运行时已从 `deepseek-v4-flash` 切换到 2026-09-10 发布的 DeepSeek-V4.1-Flash，
模型目录里的 slug 为 `deepseek-flash`。旧名保留在路由表里但不再出现在选择器，
因此历史会话仍可恢复。

## 上游契约证据

- `GET https://api.deepseek.com/models` 只返回 `deepseek-flash` 与 `deepseek-v4-pro`。
- 官方发布说明（`/news/news260910`）：V4-Flash 与 V4-Flash-Vision-Exp 已退役，
  旧名临时转发到 V4.1 Flash；V4.1 Flash 使用新架构、原生多模态。
- 官方 Models & Pricing：`deepseek-flash` 支持 Responses API、Tool Calls、JSON
  Output、Vision，上下文 1M、最大输出 384K、思考模式默认开启。
- 直接探测 `/responses`：`deepseek-flash` 返回 `status=completed`，含 reasoning
  tokens，说明与 Codex 的原生 Responses 客户端兼容。

## 变更

- `config/coexist.sh`：目录 slug 改为 `deepseek-flash`（显示名
  `DeepSeek-V4.1-Flash`），`auto_review_model_override` 指向新 slug；契约校验与
  `test-deepseek` 白名单同步更新（含旧名兼容）。
- `runtime/patches/provider_route.rs`：路由表改为
  `deepseek-flash | deepseek-v4-flash | deepseek-v4-pro`；新增旧名兼容测试。
- `runtime/router_manager.py`：补丁策略名升级为 `...v6`；协议 smoke 覆盖
  新 slug + 旧名 + Pro + GPT 四条路由；app-server smoke 默认模型改为
  `deepseek-flash`。
- 文档与技能：README、architecture、codex-mechanism、provider-switch-boundary、
  auto-review、compatibility 与两个集成 skill 的基线同步到 2026-09-10。

## 验证

- `make check`：34 项测试通过，Shell 语法与 secret scan 通过。
- 重建 release：`0.153.4-3d2ee51ca2d5-87a08119b8ef-86b55ca55bf2`（补丁
  `...v6`，`custom_sha256=6f9f3818…`），协议 smoke 结果：
  `deepseek-flash`/`deepseek-v4-flash`/`deepseek-v4-pro` → `deepseek`，
  `gpt-5.6-sol` → `openai`。
- `codex-provider test-deepseek deepseek-flash`：真实结构化工具调用通过。
- `codex-provider appserver-smoke deepseek-flash`：provider `deepseek`、
  `commandExecution`、隐藏 SHA-256 challenge 匹配。
- `codex debug models`：目录为 `deepseek-flash` + `deepseek-v4-pro`，旧 slug 已从
  选择器移除。

## 风险与待决

- V4 Pro 将于 2026-09-14 04:00 UTC 起被 DeepSeek 全部转发到 V4.1 Flash；届时
  `deepseek-v4-pro` 只是别名，需决定是否在目录中移除或等待 V4.1 Pro。
- V4.1 Flash 官方支持 Vision，但目录仍声明 `input_modalities = ["text"]`；启用前
  需要一次端到端图片验证（超出本次"换模型"范围）。
- `deepseek-v4-flash` 兼容路由是 DeepSeek 的临时安排，需要在上游停止兼容前迁移。
