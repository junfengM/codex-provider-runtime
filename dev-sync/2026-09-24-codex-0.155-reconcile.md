<!-- Generated by Codex on 2026-09-24. -->

# Codex 0.155 DeepSeek runtime reconciliation

## Result

Codex Desktop `26.917.71314` upgraded its bundled CLI from `0.153.4` to
`0.155.0-alpha.16.4`. The previous fail-closed runtime correctly refused to
activate because the resume-provider patch anchor had moved.

## Changes

- Added a guarded patch anchor for the `0.155` prepared resume-config layout.
- Preserved the legacy resume layout so earlier supported Codex tags remain
  buildable.
- Bumped the routing patch contract from `v6` to `v7`.
- Added a regression test proving provider normalization occurs before the new
  `clear_reasoning_effort` resume logic.
- Refreshed the compatibility matrix, backlog, and both bundled operator skill
  baselines.

## Validation

- Repository self-test: 35 passed.
- Upstream `codex-app-server` provider route tests: 11 passed.
- Release build: `codex-cli 0.155.0-alpha.16.4`.
- Protocol smoke: DeepSeek new thread and cold resume route to `deepseek`;
  `gpt-5.6-sol` remains on `openai`; omitted/null/empty provider history views
  agree.
- Live app-server smoke: `deepseek-flash` emitted a structured
  `commandExecution` and matched the hidden SHA-256 challenge.
- Active runtime: patch
  `deepseek-flash-pro-route-resume-and-all-provider-history-v7`, with
  `CODEX_CLI_PATH` pointing to the stable provider-runtime launcher.

## Risk

Desktop must be fully quit and reopened before an already-running process can
inherit the newly activated `CODEX_CLI_PATH`.
